[build]
  builder = "NIXPACKS"
  buildCommand = "npm install"

  [deploy]
  startCommand = "npx tsx artifacts/api-server/src/index.ts"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
  